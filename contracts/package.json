{
  "name": "moonpod-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "MoonPod ($MOONPOD) ERC-20 token, presale, and team vesting contracts.",
  "license": "MIT",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "coverage": "hardhat coverage",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "5.0.0",
    "@openzeppelin/contracts": "5.1.0",
    "dotenv": "16.4.5",
    "hardhat": "^2.28.0"
  },
  "dependencies": {
    "@nomicfoundation/edr": "^0.12.1",
    "fp-ts": "^2.16.11"
  }
}
