# Copy to .env and fill in. .env is git-ignored. NEVER commit real values.
# Private keys belong in a hardware wallet / multisig flow for production — see
# docs/security-model.md. These are for TESTNET rehearsal only.

SEPOLIA_RPC_URL=
MAINNET_RPC_URL=
DEPLOYER_PRIVATE_KEY=
ETHERSCAN_API_KEY=
REPORT_GAS=false
